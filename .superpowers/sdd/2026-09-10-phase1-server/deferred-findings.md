# Deferred findings for final-review triage

Extracted from the SDD ledger for the Phase 1 server plan. Every line below was found by a task
review, judged Minor (or explicitly deferred by a controller ruling), and recorded rather than fixed.
The controller flagged a subset as MUST-FIX-BEFORE-MERGE or as needing a product decision — those
are marked in the text. Your job is to triage: which of these block a merge, which are genuine but
can wait, and which should be closed as non-issues.

- 36:Task 1: minor (deferred): ruff UP017 in app/clock.py (`timezone.utc` → `datetime.UTC`) — resolve before/at Task 24 `ruff check`.
- 38:Task 1: minor (deferred): errors.py `_pydantic_error` jsonify(e.errors()) may hit non-serializable `ctx` values — use `e.errors(include_url=False)` + `default=str`-safe conversion when a later task exercises it.
- 39:Task 1: minor (deferred): no catch-all 500 handler producing the `{"error":…}` shape — consider at Task 24.
- 97:Task 6: minor (deferred): task-6-report.md is stale — documents only 0f130aa with 1 failing test, never the corrective commit 21fec0c or the final green run (the controller made that fix inline after the report was written).
- 98:Task 6: minor (deferred): no test at the GSM-7 multipart exact boundary (153 septets) or a mixed BMP+emoji UCS-2 boundary case.
- 102:Task 7: minor (deferred): list_for_user orders by created_at.desc() with no secondary key — tied rows are undefined SQL ordering and may differ SQLite vs Postgres. A real fix needs a monotonic column (migration), out of scope per-task; triage at the final review.
- 103:Task 7: minor (deferred): tests/test_notifications.py imports DepartmentType unused (ruff F401, brief-verbatim) — Task 24 cleanup.
- 104:Task 7: minor (noted): the diff range 21fec0c..e69ff49 includes interstitial docs commit 4c26716 (CLAUDE.md + RESUME.md), unrelated to Task 7's implementation. Controller-authored, not the implementer's.
- 110:Task 8: minor (deferred): app/__init__.py:38-40 sets RECURRING["pms.tick"] then pops it when PMS_TICK_SECONDS is falsy — redundant round-trip, brief-verbatim, harmless.
- 118:Task 9: minor (deferred): outbound.py:3 imports `select` unused (ruff F401, brief-verbatim) — Task 24 cleanup.
- 119:Task 9: minor (deferred): no test for retry()'s Conflict path (retrying a non-failed message), cross-property NotFound lookups, outbound_send's already-handled no-op guard, or registry.build_sms_adapter's ValueError branch.
- 130:Task 10: minor (deferred): classify_keyword strips only ".,!?" — "STOP;" or "STOP…" would under-match (narrower once whole-message matching lands, but still worth a look).
- 131:Task 10: minor (deferred): STOP_CONFIRMATION is a function in constant-style naming (ruff N802, plan-verbatim) — Task 24 cleanup.
- 137:Task 9: minor (deferred): retry() duplicates "set status + clear error fields + queue event" rather than routing through update_delivery_status — pre-existing, out of the ruling's scope, triage at final review.
- 142:Task 10: minor (deferred, NEW — introduced by the fix): consent.py:29 `rstrip(".,!?")` strips trailing punctuation but not whitespace AFTER it, so "STOP ." normalizes to "stop " and no longer matches STOP_WORDS. Under-matching STOP is the TCPA-risky direction, but the input is unrealistic (space then period after the keyword) and Minor findings do not extend the fix loop. Fix is one character (`rstrip(" .,!?")` or a trailing `.strip()`). FLAGGED FOR THE FINAL REVIEW AS MUST-FIX-BEFORE-MERGE and to be folded into the Task 24 cleanup pass — this is not to be silently discarded.
- 151:Task 11: minor (deferred): consent-rejection predicate duplicated between messages.py:96 and consent.assert_can_send (consent.py:57) — they agree today, but drift would silently drop audit rows rather than fail loudly.
- 152:Task 11: minor (deferred): db.get(Guest, conv.guest_id) at messages.py:95 is not property-filtered (safe — conv was property-scoped one line earlier — but departs from the global rule).
- 153:Task 11: minor (deferred): inbound.py:29-33 idempotency is check-then-insert; two genuinely concurrent carrier retries give a 500 instead of a clean 204. Same class as the Task 10 race, same fix (catch IntegrityError, re-select).
- 154:Task 11: minor (deferred): conversations.py:40 order_by(updated_at.desc()) with no tiebreak — ties under the frozen test clock are engine-dependent. Same class as the Task 7 notification-ordering minor.
- 155:Task 11: minor (noted): the plan's Interfaces line for record_inbound says `(..., *, redacted)` but Step 5 mandates `(..., *, start_sla=True)` with redaction computed internally. The CODE is right and the interface line is stale — controller to correct the plan text, not the code.
- 156:Task 11: minor (noted): report's test timings don't cohere (14 tests at 14.66s vs the full 116 at 4.31s, and 34.63s on a re-run with no change). Pass counts are the load-bearing claim and the reviewer did not dispute green; flagged only as report-hygiene.
- 167:Task 12: Ruling (Important #3 — N+1 in list) — CORRECT but fix only the safe half now. _summary runs per row over up to 50 rows issuing _last_preview and _open_wo_count plus lazy loads of c.guest and c.stay — ~200 round trips on the most-polled endpoint. Decision: add selectinload(Conversation.guest) and selectinload(Conversation.stay) now (two lines, zero SQL-semantics risk, removes half the per-row queries); DEFER folding the preview and open-WO count into correlated scalar subqueries. Rationale: that rewrite touches the filter SQL the reviewer just hand-verified correct under three-valued logic, and Phase 1 ships on SQLite against ~30 seeded conversations. Cost if wrong: the inbox stays ~100 queries per render until the deferred pass. FLAGGED FOR THE FINAL REVIEW as must-address-before-real-production-data — not a silent discard.
- 170:Task 12: minor (deferred): Message.sent_at is nullable and three orderings key on it with no nulls_first/last (_last_preview desc, detail asc, guest_thread asc) — NULL ordering defaults are OPPOSITE in SQLite and PostgreSQL and the project promises portability via DATABASE_URL. Latent (both current creation paths set sent_at) but list's own sort already uses .nulls_last(), so the file contradicts itself.
- 171:Task 12: minor (deferred): _resolve_mentions fetches every user of the property per note write then filters in Python.
- 172:Task 12: minor (deferred): two users sharing a first name are BOTH notified and both ids land in note.mentions — defensible and fails safe, but undocumented and untested (fixture first names happen to be unique).
- 173:Task 12: minor (deferred): detail and guest_thread return unbounded message history with no limit.
- 174:Task 12: minor (noted): brief is self-inconsistent — Interfaces says notes.list / patch(..., changes), Step 4/5 code says notes.list_for / patch(..., *, can_archive). Implementer followed the code blocks, which is right. Controller to correct the plan text.
- 181:Task 11: minor (deferred): if audit_write or its commit raises inside db.py's except block, the new exception replaces the in-flight ConsentError before the re-raise — a 422 would become a 500. Pre-existing class of risk (the old inline nested write could mask it identically), not introduced by this fix. Cheap hardening: wrap the audit write in its own try/except so it can never mask the original error. FLAGGED FOR THE FINAL REVIEW.
- 191:Task 13: minor (deferred): cross-property isolation for the sweep is verified by code inspection only — no regression test pins it, so a future refactor could regress it silently. Cheapest guard in the plan; worth adding at the final review.
- 192:Task 13: minor (deferred): per-conversation db.get(Guest)/db.get(Stay) inside the sweep loop is N+1 (brief-verbatim, harmless at Phase 1 scale).
- 193:Task 13: minor (deferred): sweep_once/wake_once each db.flush() after the loop although notifications.create already flushes per row — cosmetic.
- 196:Task 12: minor (noted): the re-reviewer flagged that the report's captured RED transcript shows only the `messages` assertion failing, because pytest halts a test function at its first failed assert — the `retry` line's RED wasn't separately captured. Verified genuine by inspection instead (pre-fix retry_message would 404, which also fails ==403). Reporting gap, not a defect.
- 197:Task 12: minor (deferred): send_message/add_note/patch_conversation each call conversations.get() and then a domain function that re-fetches internally — one redundant round-trip per call. Consistent pre-existing pattern, not introduced by these fixes.
- 207:Task 14: minor (deferred): assign mutates wo.status without routing through assert_transition and without writing a status_changed event — both resulting moves are legal so no illegal state is reachable, but the status history has a hole.
- 208:Task 14: minor (deferred): assign/comment/set_priority write no audit.record while create and transition do — inconsistent audit surface.
- 209:Task 14: minor (deferred): create notifies an assignee unconditionally (a supervisor assigning themselves gets "Assigned: …") while assign guards with `if user_id != actor_user_id`.
- 210:Task 14: minor (deferred): a work order with neither assignee nor department notifies nobody and is silently invisible until someone opens the queue.
- 211:Task 14: minor (deferred): DEPARTMENT_KEYWORDS ties break by dict insertion order so engineering always wins — "we need more toilet paper" scores engineering and housekeeping 1 each and routes to engineering. Prefer longest matching keyword.
- 212:Task 14: minor (deferred): description (three concatenated bodies) is unbounded in both builder and schema while title caps at 120.
- 213:Task 14: minor (deferred): OPEN_STATUSES includes `complete`, so the default "not closed" filter returns completed work orders — deliberate but the name misleads.
- 231:Task 16: minor (deferred): render's usage_count += 1 is a non-atomic read-modify-write (consistent with the existing DigitalAsset.send_count pattern in messages.py:106, so not a new deviation).
- 232:Task 16: minor (deferred): get_by_short_code ignores valid_from/valid_until, so an expired asset still resolves. design.md:343 lists the columns with no operational semantics and neither brief nor spec requires enforcement — flagged for the final review to rule on rather than assumed.
- 233:Task 16: minor (deferred): department_id on QuickReplyIn/Patch and AssetIn/Patch is not validated cross-property, unlike CategoryIn.parent_id which is.
- 237:Task 14: minor (deferred): Message.sent_at is nullable and :117 orders on it DESC — NULLs sort last on SQLite, first on PostgreSQL. The uuid tiebreak does not address this. Latent (record_inbound always sets sent_at); `.nulls_last()` is the fix. Same family as the Task 12 deferred ordering minor — the final review should sweep all of them together.
- 246:Task 15: minor (deferred): app/domain/work_orders.py:87,:113, app/domain/messages.py:84 and app/domain/notes.py:29 still call conv_domain.get directly. Now redundant from a security standpoint — the route-level get_for_viewer gates access — but they are the residue that made the original bug possible. The final review should decide whether the domain layer should refuse an unscoped conversation fetch outright.
- 251:Task 16: minor (deferred, NEW — introduced by the fix, pairs with an earlier minor): the IntegrityError catches in assets.create and quick_replies.create/update are unconditional rather than scoped to the specific unique constraint. DigitalAsset.department_id and QuickReply.department_id are FKs with no existence check, so a bogus departmentId now surfaces as a misleading "short code/shortcut already in use" 409 (after up to 20 wasted retries in assets.create) instead of an honest error. This pairs with the earlier Task 16 minor that department_id is not validated cross-property on QuickReplyIn/Patch or AssetIn/Patch — one fix closes both: validate department_id against the property before insert, mirroring CategoryIn.parent_id which already does exactly that. FLAGGED FOR THE FINAL REVIEW as a paired item; the cross-property half is a tenant-boundary inconsistency (conversations.patch and categories both validate their FK refs; these two do not).
- 254:Task 17: Ruling (last-admin / self-demotion) — ACCEPT for Phase 1, but record the real exposure rather than a comfortable one. An admin can demote or remove themselves, including the property's last admin. I checked whether `corporate` provides a recovery path and it DOES NOT: corporate is a per-property membership role like any other (permissions.py:16 grants it manage_admin, but nothing makes it cross-property), so a property whose last admin+corporate memberships are gone has NO in-app administrator — recovery would be direct DB access. Accepting anyway because: the spec is silent, the seed creates admins, this is an MVP, and CLAUDE.md forbids unrequested scope. FLAGGED FOR THE FINAL REVIEW as a genuine product gap for the user to rule on — the cheap guard is refusing to remove or demote the last membership holding manage_admin in a property. Cost if wrong: a property can be locked out of its own administration with no in-app remedy.
- 260:Task 17: minor (noted, no action): app/api/guests.py:16-19 queries Stay and Conversation by guest_id without an explicit property_id filter. Safe — guests.get() above it already filtered by property_id and a guest is not shared across properties — and it mirrors the already-accepted precedent at conversations.py:65. Same family as the Task 14 scoping ruling; consistent with how this project has ruled before.
- 271:Task 18: minor (deferred): agents() loads ALL conversations for the property with no date filter, purely as an attribute lookup — scales with property history rather than the requested window.
- 272:Task 18: minor (deferred): overview.sla_breaches is scoped to conversations CREATED in the window while agents[].sla_breaches is scoped to conversations the agent HANDLED in the window regardless of creation date. Not a leak or spec violation, but the two numbers are not comparable or summable on one dashboard, which will mislead whoever builds that screen. Worth a spec note more than a code change.
- 284:Task 19: minor (deferred): presence is keyed by user, not socket — a user with two tabs who closes one has presence cleared for both. Inherent to the brief's model; record as a known Phase 1 limitation.
- 285:Task 19: minor (deferred): app.extensions["presence_sweeper_stop"] is stored but nothing ever calls stop.set() — the sweeper cannot be stopped; dangling affordance.
- 286:Task 19: minor (deferred): no fixture resets the module-level presence.store or connections between function-scoped app fixtures; the ws tests avoid collision by hand, so a mid-test failure bleeds presence into the next test.
- 287:Task 19: minor (deferred): WS auth is evaluated once at connect, so a revoked session does not drop a live socket. Acceptable for Phase 1.
- 288:Task 19: minor (deferred): tests/test_ws.py srv.shutdown() without srv.server_close() leaks the listening socket for each of five tests.
- 299:Task 20: minor (deferred): next_events computes "today" as clock.now().date() in UTC regardless of Property.timezone, so a guest arriving today in a non-UTC property could be picked up a day early or late near local midnight. NOT a regression — it matches a codebase-wide convention (analytics buckets by UTC date too) — but it is a real edge the brief asked to be scrutinised. FLAGGED FOR THE FINAL REVIEW as a product question: should date windows be property-local?
- 300:Task 20: minor (deferred): a single module-level MockPmsAdapter means _flip toggles once per property per sweep, so one property's arrival/departure priority depends on how many properties preceded it. No cross-property data flow; fairness only.
- 313:Task 21: minor (deferred, FLAGGED FOR THE FINAL REVIEW): handle_event applies status transitions unconditionally, so POST /pms/check-out/<stay_id> can flip a stay straight from `reserved` to `checked_out` without ever passing through `checked_in`. Inherited behavior shared with the real PMS webhook path (reused per the brief's explicit instruction not to reinvent event synthesis), so not introduced here — but it means the PMS path has NO state-machine validation, unlike work orders which have TRANSITIONS/assert_transition. Worth a deliberate product decision rather than silent acceptance.
- 314:Task 21: minor (deferred): _events/_events_lock are process-global rather than per-app, so in the test process one app's /sim/events can return events from earlier unrelated app instances. Harmless for the single-dev-server use case and inherited from the brief's module-level design, but undisclosed in the report.
- 322:Task 22: minor (noted, no action): cli.py adds two click.echo lines beyond the brief's template (a status line and the login line). Disclosed, harmless, and the login line is exactly what the Task 24 one-click launcher needs to print — keep it.
- 330:Task 23: minor (noted): the report's arithmetic is wrong — it claims 37 models + 31 enums where the truth is 51 models + 17 enums = 68. The ARTIFACT is correct; only the report's self-described math is off. JobStatus is correctly absent (internal queue model, never a wire schema). Recorded so a future reader trusts the file over the report.
- 331:Task 23: minor (deferred, FLAGGED FOR THE FINAL REVIEW): the hand-maintained MODULES tuple is a standing drift risk — adding app/schemas/<new>.py in a future task and forgetting to list it fails SILENTLY, because the staleness test compares two documents that both omit it identically. Inherent to the brief's design, nothing missing today. The durable fix is to walk the package rather than list it; worth doing when the web plan starts consuming this.
- 332:Task 23: minor (deferred): two tests is thin — a third asserting a REQUEST model round-trips its camelCase aliases, or that forbid propagates to a non-guest-facing model, would distinguish "GuestThread happens to forbid extras" from "CamelModel's forbid always survives".
